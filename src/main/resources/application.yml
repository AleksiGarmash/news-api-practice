spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/newsdb
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  logging:
    level:
      com.example.news.aop.UserPermissionAspect: DEBUG
      org.springframework.web: DEBUG
      com.example.news: DEBUG

server:
  port: 8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
